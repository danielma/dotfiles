(setq dashboard-banner-logo-title "Hello!")
(setq dashboard-items '((recents . 5)
                        (projects . 5)
                        (bookmarks . 5) ;;))
                        (agenda . 5)))

(dashboard-setup-startup-hook)
