# -*- mode: snippet -*-
# name: react-component
# key: r:component
# --
import React from "react"
import { string } from "prop-types"

export default class `(s-upper-camel-case (file-name-nondirectory (file-name-sans-extension (buffer-file-name))))` extends React.Component {
  static propTypes = {
  }

  render() {
    return (
      <div>
        $0
      </div>
    )
  }
}